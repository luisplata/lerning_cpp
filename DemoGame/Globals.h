#ifndef GLOBALS_H
#define GLOBALS_H

struct Globals
{
    static int screenWidth;
    static int screenHeight;
    static float globalScale;
    static float mapScale;
    static float playerScale;
    // Añade más variables globales constantes según sea necesario
};

#endif // GLOBALS_H